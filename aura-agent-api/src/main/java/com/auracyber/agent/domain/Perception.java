package com.auracyber.agent.domain;

/**
 * Marker for whatever an agent observes on a single lifecycle tick
 * (telemetry snapshot, correlated attack tree, honeypot result, ...).
 */
public interface Perception {
}
