<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/>
	</parent>

	<groupId>com.auracyber</groupId>
	<artifactId>aura-cyber</artifactId>
	<version>0.1.0-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>AURA-Cyber</name>
	<description>Autonomous Cyber-Immune Response Ecosystem</description>

	<properties>
		<java.version>25</java.version>
		<langchain4j.version>1.13.0</langchain4j.version>
		<langchain4j-spring-boot4-starter.version>1.13.0-beta23</langchain4j-spring-boot4-starter.version>
		<pcap4j.version>1.8.2</pcap4j.version>
		<yara4j.version>0.1.0</yara4j.version>
		<neo4j.version>5.26.0</neo4j.version>
		<qdrant.version>1.13.0</qdrant.version>
		<hazelcast.version>5.5.0</hazelcast.version>
		<testcontainers.version>1.20.4</testcontainers.version>
		<archunit.version>1.3.0</archunit.version>
	</properties>

	<modules>
		<module>aura-agent-api</module>
		<module>aura-telemetry</module>
		<module>aura-memory-graph</module>
		<module>aura-agent-core</module>
		<module>aura-cce-engine</module>
		<module>aura-sandbox-executor</module>
		<module>aura-orchestrator</module>
	</modules>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.testcontainers</groupId>
				<artifactId>testcontainers-bom</artifactId>
				<version>${testcontainers.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.tngtech.archunit</groupId>
			<artifactId>archunit-junit5</artifactId>
			<version>${archunit.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<configuration>
						<release>${java.version}</release>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
</project>
